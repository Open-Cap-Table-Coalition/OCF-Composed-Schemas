{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://opencaptablecoalition.com/ocf-core/v0/core.schema.json",
  "title": "OCF Core (derived)",
  "description": "GENERATED by scripts/derive-core-build — do not hand-edit. One $def per Core-admissible (entity, variant), pruned to fields that fold losslessly to Carta, values inlined into OCF grammar. required:[] because the Carta fold declares nothing required (see docs/ocf-core-spec.md §3).",
  "oneOf": [
    {
      "$ref": "#/$defs/ConvertibleCancellation"
    },
    {
      "$ref": "#/$defs/ConvertibleConversion"
    },
    {
      "$ref": "#/$defs/ConvertibleIssuance"
    },
    {
      "$ref": "#/$defs/EquityCompensationCancellation__Option"
    },
    {
      "$ref": "#/$defs/EquityCompensationCancellation__Rsu"
    },
    {
      "$ref": "#/$defs/EquityCompensationCancellation__Sar"
    },
    {
      "$ref": "#/$defs/EquityCompensationExercise__Option"
    },
    {
      "$ref": "#/$defs/EquityCompensationExercise__Sar"
    },
    {
      "$ref": "#/$defs/EquityCompensationIssuance__Option"
    },
    {
      "$ref": "#/$defs/EquityCompensationIssuance__Rsu"
    },
    {
      "$ref": "#/$defs/EquityCompensationIssuance__Sar"
    },
    {
      "$ref": "#/$defs/EquityCompensationRelease__Rsu"
    },
    {
      "$ref": "#/$defs/EquityCompensationRepricing__Option"
    },
    {
      "$ref": "#/$defs/EquityCompensationRepricing__Sar"
    },
    {
      "$ref": "#/$defs/Issuer"
    },
    {
      "$ref": "#/$defs/Stakeholder"
    },
    {
      "$ref": "#/$defs/StakeholderRelationshipChangeEvent"
    },
    {
      "$ref": "#/$defs/StockCancellation__Default"
    },
    {
      "$ref": "#/$defs/StockCancellation__Rsa"
    },
    {
      "$ref": "#/$defs/StockClass"
    },
    {
      "$ref": "#/$defs/StockClassAuthorizedSharesAdjustment"
    },
    {
      "$ref": "#/$defs/StockIssuance__Default"
    },
    {
      "$ref": "#/$defs/StockIssuance__Rsa"
    },
    {
      "$ref": "#/$defs/StockPlan"
    },
    {
      "$ref": "#/$defs/Valuation"
    },
    {
      "$ref": "#/$defs/VestingTerms"
    },
    {
      "$ref": "#/$defs/WarrantCancellation"
    },
    {
      "$ref": "#/$defs/WarrantIssuance"
    },
    {
      "$ref": "#/$defs/WarrantTransfer"
    }
  ],
  "$defs": {
    "ConvertibleCancellation": {
      "type": "object",
      "title": "ConvertibleCancellation",
      "properties": {
        "amount": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "amount": {
              "type": "string",
              "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
            },
            "currency": {
              "type": "string",
              "pattern": "^[A-Z]{3}$"
            }
          },
          "required": [
            "amount",
            "currency"
          ],
          "description": "Amount of monetary value cancelled"
        },
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date on which the transaction occurred"
        },
        "security_id": {
          "type": "string",
          "description": "Identifier for the security (stock, plan security, warrant, or convertible) by which it can be referenced by other transaction objects. N…"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "ConvertibleConversion": {
      "type": "object",
      "title": "ConvertibleConversion",
      "properties": {
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date on which the transaction occurred"
        },
        "quantity_converted": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "Quantity of security units converted"
        },
        "security_id": {
          "type": "string",
          "description": "Identifier for the security (stock, plan security, warrant, or convertible) by which it can be referenced by other transaction objects. N…"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "ConvertibleIssuance": {
      "type": "object",
      "title": "ConvertibleIssuance",
      "properties": {
        "convertible_type": {
          "type": "string",
          "enum": [
            "NOTE",
            "SAFE",
            "CONVERTIBLE_SECURITY"
          ],
          "description": "What kind of convertible instrument is this (of the supported, enumerated types)"
        },
        "custom_id": {
          "type": "string",
          "description": "A custom ID for this security (e.g. CN-1.)"
        },
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date on which the transaction occurred"
        },
        "investment_amount": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "amount": {
              "type": "string",
              "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
            },
            "currency": {
              "type": "string",
              "pattern": "^[A-Z]{3}$"
            }
          },
          "required": [
            "amount",
            "currency"
          ],
          "description": "Amount invested and outstanding on date of issuance of this convertible"
        },
        "security_id": {
          "type": "string",
          "description": "Identifier for the security (stock, plan security, warrant, or convertible) by which it can be referenced by other transaction objects. N…"
        },
        "stakeholder_id": {
          "type": "string",
          "description": "Identifier for the stakeholder that holds legal title to this security"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "EquityCompensationCancellation__Option": {
      "type": "object",
      "title": "EquityCompensationCancellation (Option)",
      "properties": {
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date on which the transaction occurred"
        },
        "quantity": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "Quantity of non-monetary security units cancelled"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "EquityCompensationCancellation__Rsu": {
      "type": "object",
      "title": "EquityCompensationCancellation (Rsu)",
      "properties": {
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date on which the transaction occurred"
        },
        "quantity": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "Quantity of non-monetary security units cancelled"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "EquityCompensationCancellation__Sar": {
      "type": "object",
      "title": "EquityCompensationCancellation (Sar)",
      "properties": {
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date on which the transaction occurred"
        },
        "quantity": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "Quantity of non-monetary security units cancelled"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "EquityCompensationExercise__Option": {
      "type": "object",
      "title": "EquityCompensationExercise (Option)",
      "properties": {
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date on which the transaction occurred"
        },
        "quantity": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "Quantity of shares exercised"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "EquityCompensationExercise__Sar": {
      "type": "object",
      "title": "EquityCompensationExercise (Sar)",
      "properties": {
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date on which the transaction occurred"
        },
        "quantity": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "Quantity of shares exercised"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "EquityCompensationIssuance__Option": {
      "type": "object",
      "title": "EquityCompensationIssuance (Option)",
      "properties": {
        "board_approval_date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date of board approval for the security, when applicable."
        },
        "compensation_type": {
          "type": "string",
          "enum": [
            "OPTION_NSO",
            "OPTION_ISO",
            "OPTION",
            "RSU",
            "CSAR",
            "SSAR"
          ],
          "description": "The kind of equity compensation. Determines which type-specific fields are required."
        },
        "custom_id": {
          "type": "string",
          "description": "Human-readable identifier for the security (e.g. 'CN-1')."
        },
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date on which the issuance transaction occurred. Distinct from the vesting commencement date, which is carried separately as `vesting_sta…"
        },
        "early_exercisable": {
          "type": "boolean",
          "description": "If true, the security is exercisable prior to completion of vesting; the schedule then governs a right-of-repurchase lapse rather than th…"
        },
        "exercise_price": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "amount": {
              "type": "string",
              "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
            },
            "currency": {
              "type": "string",
              "pattern": "^[A-Z]{3}$"
            }
          },
          "required": [
            "amount",
            "currency"
          ],
          "description": "Required for option compensation types. The price per share at which the option can be exercised."
        },
        "expiration_date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Expiration date of the security, or null if it does not expire."
        },
        "option_grant_type": {
          "type": "string",
          "enum": [
            "NSO",
            "ISO",
            "INTL"
          ],
          "description": "If the security is an option, what kind. Retained from v1 for compatibility; in the new model this has been incorporated into Compensatio…"
        },
        "quantity": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "Number of shares subject to this security."
        },
        "security_id": {
          "type": "string",
          "description": "Identifier for the security created by this issuance. Other transactions (vesting event, exercise, cancellation, etc.) reference this id."
        },
        "stakeholder_id": {
          "type": "string",
          "description": "Identifier of the stakeholder holding legal title to the security."
        },
        "stock_class_id": {
          "type": "string",
          "description": "The stock class the security exercises/settles into. Important for plan-less options and plans that support multiple share classes."
        },
        "stock_plan_id": {
          "type": "string",
          "description": "If the security was issued from a stock plan, the plan's id. Plan-less options are valid and omit this field."
        },
        "vesting_start_date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "The per-grant vesting commencement date — the anchor every statement of the referenced template grids from. Required whenever `vesting_te…"
        },
        "vesting_template_id": {
          "type": "string",
          "description": "Identifier of the v2 vesting template the security is subject to. If neither `vesting_template_id` nor `vestings` is present, the securit…"
        },
        "vestings": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "date": {
                "type": "string",
                "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
              },
              "amount": {
                "type": "string",
                "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
              }
            },
            "required": [
              "date",
              "amount"
            ]
          },
          "description": "Optional materialized projection of exact vesting dates and amounts. A grant may be described by the declarative template (`vesting_templ…"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "EquityCompensationIssuance__Rsu": {
      "type": "object",
      "title": "EquityCompensationIssuance (Rsu)",
      "properties": {
        "board_approval_date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date of board approval for the security, when applicable."
        },
        "custom_id": {
          "type": "string",
          "description": "Human-readable identifier for the security (e.g. 'CN-1')."
        },
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date on which the issuance transaction occurred. Distinct from the vesting commencement date, which is carried separately as `vesting_sta…"
        },
        "quantity": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "Number of shares subject to this security."
        },
        "security_id": {
          "type": "string",
          "description": "Identifier for the security created by this issuance. Other transactions (vesting event, exercise, cancellation, etc.) reference this id."
        },
        "stakeholder_id": {
          "type": "string",
          "description": "Identifier of the stakeholder holding legal title to the security."
        },
        "stock_class_id": {
          "type": "string",
          "description": "The stock class the security exercises/settles into. Important for plan-less options and plans that support multiple share classes."
        },
        "stock_plan_id": {
          "type": "string",
          "description": "If the security was issued from a stock plan, the plan's id. Plan-less options are valid and omit this field."
        },
        "vesting_start_date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "The per-grant vesting commencement date — the anchor every statement of the referenced template grids from. Required whenever `vesting_te…"
        },
        "vesting_template_id": {
          "type": "string",
          "description": "Identifier of the v2 vesting template the security is subject to. If neither `vesting_template_id` nor `vestings` is present, the securit…"
        },
        "vestings": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "date": {
                "type": "string",
                "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
              },
              "amount": {
                "type": "string",
                "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
              }
            },
            "required": [
              "date",
              "amount"
            ]
          },
          "description": "Optional materialized projection of exact vesting dates and amounts. A grant may be described by the declarative template (`vesting_templ…"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "EquityCompensationIssuance__Sar": {
      "type": "object",
      "title": "EquityCompensationIssuance (Sar)",
      "properties": {
        "base_price": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "amount": {
              "type": "string",
              "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
            },
            "currency": {
              "type": "string",
              "pattern": "^[A-Z]{3}$"
            }
          },
          "required": [
            "amount",
            "currency"
          ],
          "description": "Required for stock appreciation right compensation types (CSAR, SSAR). The base price used to calculate appreciation."
        },
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date on which the issuance transaction occurred. Distinct from the vesting commencement date, which is carried separately as `vesting_sta…"
        },
        "expiration_date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Expiration date of the security, or null if it does not expire."
        },
        "quantity": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "Number of shares subject to this security."
        },
        "stock_class_id": {
          "type": "string",
          "description": "The stock class the security exercises/settles into. Important for plan-less options and plans that support multiple share classes."
        },
        "stock_plan_id": {
          "type": "string",
          "description": "If the security was issued from a stock plan, the plan's id. Plan-less options are valid and omit this field."
        },
        "vesting_template_id": {
          "type": "string",
          "description": "Identifier of the v2 vesting template the security is subject to. If neither `vesting_template_id` nor `vestings` is present, the securit…"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "EquityCompensationRelease__Rsu": {
      "type": "object",
      "title": "EquityCompensationRelease (Rsu)",
      "properties": {
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date on which the transaction occurred"
        },
        "quantity": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "Quantity of shares released"
        },
        "release_price": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "amount": {
              "type": "string",
              "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
            },
            "currency": {
              "type": "string",
              "pattern": "^[A-Z]{3}$"
            }
          },
          "required": [
            "amount",
            "currency"
          ],
          "description": "The release price used to determine the value of the security at the time of release"
        },
        "settlement_date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "The settlement date for the shares released, typically after the release transaction date"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "EquityCompensationRepricing__Option": {
      "type": "object",
      "title": "EquityCompensationRepricing (Option)",
      "properties": {
        "new_exercise_price": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "amount": {
              "type": "string",
              "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
            },
            "currency": {
              "type": "string",
              "pattern": "^[A-Z]{3}$"
            }
          },
          "required": [
            "amount",
            "currency"
          ],
          "description": "What is the exercise price of the option after the repricing?"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "EquityCompensationRepricing__Sar": {
      "type": "object",
      "title": "EquityCompensationRepricing (Sar)",
      "properties": {
        "new_exercise_price": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "amount": {
              "type": "string",
              "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
            },
            "currency": {
              "type": "string",
              "pattern": "^[A-Z]{3}$"
            }
          },
          "required": [
            "amount",
            "currency"
          ],
          "description": "What is the exercise price of the option after the repricing?"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "Issuer": {
      "type": "object",
      "title": "Issuer",
      "properties": {
        "dba": {
          "type": "string",
          "description": "Doing Business As name"
        },
        "legal_name": {
          "type": "string",
          "description": "Legal name of the issuer"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "Stakeholder": {
      "type": "object",
      "title": "Stakeholder",
      "properties": {
        "current_relationship": {
          "type": "string",
          "enum": [
            "ADVISOR",
            "BOARD_MEMBER",
            "CONSULTANT",
            "EMPLOYEE",
            "EX_ADVISOR",
            "EX_CONSULTANT",
            "EX_EMPLOYEE",
            "EXECUTIVE",
            "FOUNDER",
            "INVESTOR",
            "NON_US_EMPLOYEE",
            "OFFICER",
            "OTHER"
          ],
          "description": "What is the current relationship of the stakeholder to the issuer?"
        },
        "current_relationships": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "ADVISOR",
              "BOARD_MEMBER",
              "CONSULTANT",
              "EMPLOYEE",
              "EX_ADVISOR",
              "EX_CONSULTANT",
              "EX_EMPLOYEE",
              "EXECUTIVE",
              "FOUNDER",
              "INVESTOR",
              "NON_US_EMPLOYEE",
              "OFFICER",
              "OTHER"
            ]
          },
          "description": "What is/are the current relationship(s) of the stakeholder to the issuer?"
        },
        "issuer_assigned_id": {
          "type": "string",
          "description": "This might be any sort of id assigned to the stakeholder by the issuer, such as an internal company ID for an employee stakeholder"
        },
        "stakeholder_type": {
          "type": "string",
          "enum": [
            "INDIVIDUAL",
            "INSTITUTION"
          ],
          "description": "Distinguish individuals from institutions"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "StakeholderRelationshipChangeEvent": {
      "type": "object",
      "title": "StakeholderRelationshipChangeEvent",
      "properties": {
        "relationship_ended": {
          "type": "string",
          "enum": [
            "ADVISOR",
            "BOARD_MEMBER",
            "CONSULTANT",
            "EMPLOYEE",
            "EX_ADVISOR",
            "EX_CONSULTANT",
            "EX_EMPLOYEE",
            "EXECUTIVE",
            "FOUNDER",
            "INVESTOR",
            "NON_US_EMPLOYEE",
            "OFFICER",
            "OTHER"
          ],
          "description": "Denoting the ending of this relationship on the change date"
        },
        "relationship_started": {
          "type": "string",
          "enum": [
            "ADVISOR",
            "BOARD_MEMBER",
            "CONSULTANT",
            "EMPLOYEE",
            "EX_ADVISOR",
            "EX_CONSULTANT",
            "EX_EMPLOYEE",
            "EXECUTIVE",
            "FOUNDER",
            "INVESTOR",
            "NON_US_EMPLOYEE",
            "OFFICER",
            "OTHER"
          ],
          "description": "Denoting the beginning of this relationship on the change date"
        },
        "stakeholder_id": {
          "type": "string",
          "description": "Identifier of the Stakeholder object, a subject of this change event \"transaction\""
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "StockCancellation__Default": {
      "type": "object",
      "title": "StockCancellation (Default)",
      "properties": {
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date on which the transaction occurred"
        },
        "quantity": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "Quantity of non-monetary security units cancelled"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "StockCancellation__Rsa": {
      "type": "object",
      "title": "StockCancellation (Rsa)",
      "properties": {
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date on which the transaction occurred"
        },
        "quantity": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "Quantity of non-monetary security units cancelled"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "StockClass": {
      "type": "object",
      "title": "StockClass",
      "properties": {
        "class_type": {
          "type": "string",
          "enum": [
            "COMMON",
            "PREFERRED"
          ],
          "description": "The type of this stock class (e.g. Preferred or Common)"
        },
        "default_id_prefix": {
          "type": "string",
          "description": "Default prefix for certificate numbers in certificated shares (e.g. CS- in CS-1). If certificate IDs have a dash, the prefix should end i…"
        },
        "initial_shares_authorized": {
          "type": "string",
          "description": "The initial number of shares authorized for this stock class"
        },
        "liquidation_preference_multiple": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "The liquidation preference per share for this stock class"
        },
        "name": {
          "type": "string",
          "description": "Name for the stock type (e.g. Series A Preferred or Class A Common)"
        },
        "par_value": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "amount": {
              "type": "string",
              "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
            },
            "currency": {
              "type": "string",
              "pattern": "^[A-Z]{3}$"
            }
          },
          "required": [
            "amount",
            "currency"
          ],
          "description": "Per-share par value of this stock class"
        },
        "participation_cap_multiple": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "The participation cap multiple per share for this stock class"
        },
        "price_per_share": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "amount": {
              "type": "string",
              "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
            },
            "currency": {
              "type": "string",
              "pattern": "^[A-Z]{3}$"
            }
          },
          "required": [
            "amount",
            "currency"
          ],
          "description": "Per-share price this stock class was issued for"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "StockClassAuthorizedSharesAdjustment": {
      "type": "object",
      "title": "StockClassAuthorizedSharesAdjustment",
      "properties": {
        "new_shares_authorized": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "The new number of shares authorized for this stock class as of the event of this transaction"
        },
        "stock_class_id": {
          "type": "string",
          "description": "Identifier of the StockClass object, a subject of this transaction"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "StockIssuance__Default": {
      "type": "object",
      "title": "StockIssuance (Default)",
      "properties": {
        "cost_basis": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "amount": {
              "type": "string",
              "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
            },
            "currency": {
              "type": "string",
              "pattern": "^[A-Z]{3}$"
            }
          },
          "required": [
            "amount",
            "currency"
          ],
          "description": "The cost basis for this particular stock"
        },
        "custom_id": {
          "type": "string",
          "description": "A custom ID for this security (e.g. CN-1.)"
        },
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date on which the transaction occurred"
        },
        "quantity": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "Number of shares issued to the stakeholder"
        },
        "security_id": {
          "type": "string",
          "description": "Identifier for the security (stock, plan security, warrant, or convertible) by which it can be referenced by other transaction objects. N…"
        },
        "share_price": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "amount": {
              "type": "string",
              "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
            },
            "currency": {
              "type": "string",
              "pattern": "^[A-Z]{3}$"
            }
          },
          "required": [
            "amount",
            "currency"
          ],
          "description": "The price per share paid for the stock by the holder"
        },
        "stakeholder_id": {
          "type": "string",
          "description": "Identifier for the stakeholder that holds legal title to this security"
        },
        "stock_class_id": {
          "type": "string",
          "description": "Identifier of the stock class for this stock issuance"
        },
        "stock_plan_id": {
          "type": "string",
          "description": "Identifier of StockPlan the Stock was issued from (in the case of RSAs or Stock issued from a plan)."
        },
        "vesting_terms_id": {
          "type": "string",
          "description": "Identifier of the VestingTerms to which this security is subject. If neither `vesting_terms_id` or `vestings` are present then the securi…"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "StockIssuance__Rsa": {
      "type": "object",
      "title": "StockIssuance (Rsa)",
      "properties": {
        "board_approval_date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date of board approval for the security"
        },
        "cost_basis": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "amount": {
              "type": "string",
              "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
            },
            "currency": {
              "type": "string",
              "pattern": "^[A-Z]{3}$"
            }
          },
          "required": [
            "amount",
            "currency"
          ],
          "description": "The cost basis for this particular stock"
        },
        "custom_id": {
          "type": "string",
          "description": "A custom ID for this security (e.g. CN-1.)"
        },
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date on which the transaction occurred"
        },
        "quantity": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "Number of shares issued to the stakeholder"
        },
        "security_id": {
          "type": "string",
          "description": "Identifier for the security (stock, plan security, warrant, or convertible) by which it can be referenced by other transaction objects. N…"
        },
        "share_price": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "amount": {
              "type": "string",
              "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
            },
            "currency": {
              "type": "string",
              "pattern": "^[A-Z]{3}$"
            }
          },
          "required": [
            "amount",
            "currency"
          ],
          "description": "The price per share paid for the stock by the holder"
        },
        "stakeholder_id": {
          "type": "string",
          "description": "Identifier for the stakeholder that holds legal title to this security"
        },
        "stock_class_id": {
          "type": "string",
          "description": "Identifier of the stock class for this stock issuance"
        },
        "stock_plan_id": {
          "type": "string",
          "description": "Identifier of StockPlan the Stock was issued from (in the case of RSAs or Stock issued from a plan)."
        },
        "vesting_terms_id": {
          "type": "string",
          "description": "Identifier of the VestingTerms to which this security is subject. If neither `vesting_terms_id` or `vestings` are present then the securi…"
        },
        "vestings": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "date": {
                "type": "string",
                "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
              },
              "amount": {
                "type": "string",
                "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
              }
            },
            "required": [
              "date",
              "amount"
            ]
          },
          "description": "List of exact vesting dates and amounts for this security. When `vestings` array is present then `vesting_terms_id` may be ignored."
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "StockPlan": {
      "type": "object",
      "title": "StockPlan",
      "properties": {
        "initial_shares_reserved": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "The initial number of shares reserved in the pool for this stock plan by the Board or equivalent body."
        },
        "plan_name": {
          "type": "string",
          "description": "Name for the stock plan"
        },
        "stock_class_id": {
          "type": "string",
          "description": "[DEPRECATED in favor of stock_class_ids] Identifier of the StockClass object this plan is composed of."
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "Valuation": {
      "type": "object",
      "title": "Valuation",
      "properties": {
        "price_per_share": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "amount": {
              "type": "string",
              "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
            },
            "currency": {
              "type": "string",
              "pattern": "^[A-Z]{3}$"
            }
          },
          "required": [
            "amount",
            "currency"
          ],
          "description": "Valued price per share"
        },
        "stock_class_id": {
          "type": "string",
          "description": "Identifier of the stock class for this valuation"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "VestingTerms": {
      "type": "object",
      "title": "VestingTerms",
      "properties": {
        "id": {
          "type": "string",
          "description": "Identifier the issuance's `vesting_template_id` references."
        },
        "statements": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "order": {
                "type": "integer"
              },
              "schedule": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "occurrences": {
                    "type": "integer"
                  },
                  "period": {
                    "type": "integer"
                  },
                  "period_type": {
                    "type": "string",
                    "enum": [
                      "DAYS",
                      "MONTHS",
                      "YEARS"
                    ]
                  },
                  "cliff": {
                    "type": "object",
                    "additionalProperties": false,
                    "properties": {
                      "length": {
                        "type": "integer"
                      },
                      "period_type": {
                        "type": "string",
                        "enum": [
                          "DAYS",
                          "MONTHS",
                          "YEARS"
                        ]
                      },
                      "percentage": {
                        "type": "string",
                        "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
                      }
                    },
                    "required": [
                      "length",
                      "period_type",
                      "percentage"
                    ]
                  }
                },
                "required": [
                  "occurrences",
                  "period",
                  "period_type"
                ]
              },
              "event_condition": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "event_id": {
                    "type": "string"
                  }
                },
                "required": [
                  "event_id"
                ]
              },
              "percentage": {
                "type": "string",
                "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
              }
            },
            "required": [
              "order",
              "percentage"
            ]
          },
          "description": "Ordered list of vesting statements. They chain implicitly by their `order` field rather than via explicit graph edges."
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "WarrantCancellation": {
      "type": "object",
      "title": "WarrantCancellation",
      "properties": {
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date on which the transaction occurred"
        },
        "quantity": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "Quantity of non-monetary security units cancelled"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "WarrantIssuance": {
      "type": "object",
      "title": "WarrantIssuance",
      "properties": {
        "custom_id": {
          "type": "string",
          "description": "A custom ID for this security (e.g. CN-1.)"
        },
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date on which the transaction occurred"
        },
        "exercise_price": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "amount": {
              "type": "string",
              "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
            },
            "currency": {
              "type": "string",
              "pattern": "^[A-Z]{3}$"
            }
          },
          "required": [
            "amount",
            "currency"
          ],
          "description": "The exercise price of the warrant"
        },
        "purchase_price": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "amount": {
              "type": "string",
              "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$"
            },
            "currency": {
              "type": "string",
              "pattern": "^[A-Z]{3}$"
            }
          },
          "required": [
            "amount",
            "currency"
          ],
          "description": "Actual purchase price of the warrant (sum up purported value of all consideration, including in-kind)"
        },
        "quantity": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "Quantity of shares the warrant is exercisable for"
        },
        "security_id": {
          "type": "string",
          "description": "Identifier for the security (stock, plan security, warrant, or convertible) by which it can be referenced by other transaction objects. N…"
        },
        "stakeholder_id": {
          "type": "string",
          "description": "Identifier for the stakeholder that holds legal title to this security"
        },
        "vesting_terms_id": {
          "type": "string",
          "description": "Identifier of the VestingTerms to which this security is subject. If neither `vesting_terms_id` or `vestings` are present then the securi…"
        },
        "warrant_expiration_date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "What is expiration date of the warrant (if applicable)"
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "WarrantTransfer": {
      "type": "object",
      "title": "WarrantTransfer",
      "properties": {
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date on which the transaction occurred"
        },
        "quantity": {
          "type": "string",
          "pattern": "^[+-]?[0-9]+(\\.[0-9]{1,10})?$",
          "description": "Quantity of non-monetary security units transferred"
        },
        "security_id": {
          "type": "string",
          "description": "Identifier for the security (stock, plan security, warrant, or convertible) by which it can be referenced by other transaction objects. N…"
        }
      },
      "required": [],
      "additionalProperties": false
    }
  }
}
